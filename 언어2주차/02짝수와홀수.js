function solution1(num) {
    let answer = num%2 ==0 ? "Even" : "Odd" 
    return answer
} 

function solution2(num) {
  return num%2 ==0 ? "Even" : "Odd" 
} 
// "return" ==> 함수의 처리 결과를 반환하는 것 
// [변수 이름]으로 내보내나, [값]을 내보내나 똑같은거에요.  
